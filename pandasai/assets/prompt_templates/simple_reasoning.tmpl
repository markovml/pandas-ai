
At the end, declare "result" variable as a dictionary of type and value.
{viz_library_type}
{instructions}

Generate python code and return full updated code:
NOTE: If the request is to plot, show, or display something similar generate "result" of type "high-chart" only.
Make sure that for responses that can be plotted in a table such as correlations and similar return a valid dataframe.
For responses that can be presented in tabular form, like correlations, return a dataframe.
Do not use python libraries like matplotlib or plotly or similar.

